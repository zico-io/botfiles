# git — CLI patterns & workarounds

One file per tool. Commands, configs, and gotchas worth remembering.

- `git init` starts the tracked SSOT; `.botfile/derived/` is gitignored <source: seed, 2026-07-07>
