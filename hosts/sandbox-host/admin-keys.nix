# SSH public keys for the `admin` user. Public keys are safe to commit.
#
# Tailscale SSH (modules/tailnet.nix) is the primary access path, so this list
# may stay empty: it is the fallback for reaching the box over the tailnet when
# tailnet SSH policy itself is the thing being changed.
[
]
